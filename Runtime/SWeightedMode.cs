namespace Sperlich.Easing {

	/// <summary>Mirrors <see cref="UnityEngine.WeightedMode"/> value-for-value so casting between the two is lossless.</summary>
	public enum SWeightedMode {
		None = 0,
		In = 1,
		Out = 2,
		Both = 3,
	}
}
